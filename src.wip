const power = (a: int, b: int): int => a ** b;

const sum = (a: int, b: int): int => {
  const c = a + b;
  return c;
};

const div = (a: int, b: int):int => {
  return a / b;
};

var sum_result = sum(33, 36);

print(sum_result);
