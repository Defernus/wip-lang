
const a = 2;
const b = 3 + 5;

const printSum = (left: int, right: int) => {
  const result = left + right;
  printInt(result);
};

if (a) {
  printInt(1111);
}

const zero = 0;
if zero {
  printInt(222222);
}
